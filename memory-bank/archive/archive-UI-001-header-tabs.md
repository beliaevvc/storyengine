# АРХИВ ЗАДАЧИ: UI-001 — Перенос табов в хедер

## МЕТАДАННЫЕ

| Поле | Значение |
|------|----------|
| Task ID | UI-001 |
| Название | Перенос табов в хедер |
| Уровень сложности | 2 (Basic Enhancement) |
| Статус | ✅ COMPLETE |
| Дата начала | 2026-01-17 |
| Дата завершения | 2026-01-17 |

---

## КРАТКОЕ ОПИСАНИЕ

Рефакторинг UI для улучшения визуальной иерархии: перенос навигационных табов режимов работы (Редактор, Сюжет, Персонажи, Таймлайн) из WorkspacePanel в центр хедера, удаление устаревших breadcrumbs и исправление визуальных артефактов.

---

## ТРЕБОВАНИЯ

### Запрос пользователя
1. Перенести табы режимов в хедер по центру "как-то красиво"
2. Убрать breadcrumbs ("вот это вообще кажется не надо")
3. Убрать лишнюю линию в области вкладок документов

### Технические требования
- Сохранить функциональность переключения режимов
- Обеспечить адаптивность на разных размерах экрана
- Минимальные изменения в существующем коде

---

## РЕАЛИЗАЦИЯ

### Архитектурное решение
State lifting — состояние активного режима поднято на уровень страницы (`page.tsx`) и передаётся через props в Header и WorkspacePanel.

```
page.tsx (activeMode state)
    ├── AppLayout
    │       └── Header (показывает табы, onChange)
    └── WorkspacePanel (рендерит контент по activeMode)
```

### Изменённые файлы

| Файл | Изменение |
|------|-----------|
| `src/presentation/components/layout/Header.tsx` | +MODE_TABS массив, +центрированные табы с иконками |
| `src/presentation/components/layout/AppLayout.tsx` | +props activeMode, onModeChange |
| `src/presentation/components/workspace/WorkspacePanel.tsx` | -MODE_TABS, -tab bar UI, activeMode как prop |
| `src/presentation/components/workspace/index.ts` | +export WorkspaceMode type |
| `src/app/(dashboard)/projects/[projectId]/page.tsx` | +useState для activeMode |
| `src/presentation/components/editor/Editor.tsx` | -Breadcrumbs, -projectName/documentName props |
| `src/presentation/components/workspace/DocumentTabs.tsx` | -border-b из spacer |

### Ключевые изменения кода

**Header.tsx** — добавлены табы:
```tsx
const MODE_TABS = [
  { id: 'editor', label: 'Редактор', icon: FileText },
  { id: 'plot', label: 'Сюжет', icon: GitBranch },
  { id: 'characters', label: 'Персонажи', icon: Users },
  { id: 'timeline', label: 'Таймлайн', icon: Clock },
];
```

**Стилизация активного таба:**
```tsx
activeMode === tab.id
  ? 'bg-accent text-white shadow-sm'
  : 'text-fg-muted hover:text-fg hover:bg-surface-hover'
```

---

## ТЕСТИРОВАНИЕ

### Ручная проверка
- [x] Табы отображаются в центре хедера
- [x] Переключение режимов работает
- [x] Breadcrumbs удалены
- [x] Линия в DocumentTabs убрана
- [x] Адаптивность на узких экранах (скрываются лейблы)

### TypeScript
- [x] Нет ошибок компиляции
- [x] Корректный экспорт типов

---

## УРОКИ

1. **State Lifting** — когда несколько компонентов зависят от одного состояния, поднимайте его на общего родителя
2. **Props Drilling** — для 2-3 уровней вложенности props drilling приемлем, для большего — используйте context или store
3. **Адаптивный UI** — `hidden sm:inline` паттерн для скрытия текста на мобильных

---

## ССЫЛКИ

| Документ | Путь |
|----------|------|
| Рефлексия | `memory-bank/reflection/reflection-UI-001-header-tabs.md` |

---

## СКРИНШОТ РЕЗУЛЬТАТА

Табы в хедере:
```
┌─────────────────────────────────────────────────────────────┐
│ 📖 StoryEngine / Проект   [Редактор][Сюжет][Персонажи][⏱]  │
└─────────────────────────────────────────────────────────────┘
```
